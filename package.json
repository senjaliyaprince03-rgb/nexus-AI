{
  "name": "nexusai-workspace",
  "private": true,
  "scripts": {
    "gateway": "python nexus_master_api.py",
    "validate:integrations": "python scripts/validate_integrations.py",
    "dev": "node scripts/dev.js",
    "build": "npm --prefix nexusai/frontend run build",
    "start": "npm --prefix nexusai/frontend run start",
    "lint": "npm --prefix nexusai/frontend run lint",
    "type-check": "npm --prefix nexusai/frontend run type-check",
    "test": "npm --prefix nexusai/frontend run test",
    "test:e2e": "npm --prefix nexusai/frontend run test:e2e"
  },
  "dependencies": {
    "firebase": "^12.14.0"
  }
}
