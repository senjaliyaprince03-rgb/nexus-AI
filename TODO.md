# TODO

- [ ] Replace hard-coded dashboard/sidebar/topbar/cookie strings with i18n `t(key)` calls.
- [ ] Add missing translation keys to `nexusai/frontend/src/locales/translations.ts`.
- [ ] Update `app/dashboard/page.tsx` fallbackOverview strings (label/description) to use translation keys or make them localized.
- [ ] Ensure language switch re-renders all pages (verify components use `useI18nStore().t`).
- [ ] Run frontend tests/build/lint if available.

